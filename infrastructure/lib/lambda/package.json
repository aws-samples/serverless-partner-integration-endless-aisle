{
  "name": "lambda-handlers",
  "version": "0.0.0",
  "description": "Code for all lambda handlers",
  "scripts": {
    "build": "tsc",
    "package": "npm ci && npm run build",
    "clean": "rm -rf node_modules && rm */*.d.ts && rm */*.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "*"
  },
  "dependencies": {
    "axios": "*"
  }
}